"""Golden-test verification harness (REBUILD_SPEC §6).

R-эталон используется **офлайн**: фикстуры один раз генерируются и коммитятся в
``src/verification/golden/*.json``; тесты (`tests/golden/`) сверяются с дампами и
**не требуют установленного R**.

Поскольку R в окружении сборки отсутствует, роль «эталона» выполняет
:mod:`src.verification.reference` — независимая, математически-точная реализация
тех же величин (другой кодовый путь, чем продакшн): QR-OLS, явные критерии
D/I-оптимальности, формулы Деррингера–Сюича, текстбук-GP. Для прослеживаемости в
каждую фикстуру вложен эквивалентный R-сниппет (`r_reference`), дающий те же числа.

Слои:
  * ``reference.py``      — независимый эталон (ground truth).
  * ``golden_io.py``      — сохранение/загрузка JSON-фикстур (+ реестр допусков).
  * ``generate_golden.py``— построение фикстур (запускается вручную/в CI).
  * ``golden/*.json``     — закоммиченные фикстуры.
"""
