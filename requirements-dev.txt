# Development dependencies
pytest>=6.0
pytest-cov>=2.0
black>=21.0
flake8>=3.9
mypy>=0.9
ipython>=7.0
jupyter>=1.0
pre-commit>=2.0
# M9 — MCP-интроспекция (сервер doe-introspect). Только для запуска сервера;
# чистая логика src/mcp/queries.py и trace тестируются без этого пакета.
mcp>=1.2.0


